---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: pocket-id
  name: pocket-id-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi