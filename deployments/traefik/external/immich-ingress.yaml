apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: immich
  namespace: default
  annotations:
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`www.immich.local.tigovanroy.space`)
      kind: Rule
      services:
        - name: immich-server
          port: 30041  # Replace with the correct port if needed
          scheme: http  # Assuming HTTP; use https if Immich is using HTTPS
          passHostHeader: true
    - match: Host(`immich.local.tigovanroy.space`)
      kind: Rule
      services:
        - name: immich-server
          port: 30041  # Replace with the correct port if needed
          scheme: http
          passHostHeader: true
      middlewares:
        - name: default-headers
  tls:
    secretName: local-tigovanroy-space-production-tls  # Same TLS cert used for other services
