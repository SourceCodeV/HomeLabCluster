apiVersion: v1
kind: Service
metadata:
  name: prowlarr-service
  namespace: media
spec:
  type: ClusterIP
  ports:
    - port: 9696
      targetPort: prowlarr
  selector:
    app: prowlarr
    part-of: prowlarr
